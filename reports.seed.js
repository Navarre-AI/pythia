// reports.seed.js — a fresh install starts with NO reports. Pythia grows them
// from the actual data ("Generate reports from my data") or from what the user
// asks and saves in chat. A report is { id, name, description, sql, chart? }.

export const SEED_REPORTS = [];
